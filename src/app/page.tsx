'use client';
import { PageContainer } from "@/components";
import { CredentialsForm } from "@/app/credentials-form.component";

export default function Credentials() {
  return (
    <PageContainer>
      <CredentialsForm />
    </PageContainer>
  );
}