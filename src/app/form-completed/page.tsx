'use client';
import { PageContainer, FormValueDisplay } from '@/components';

export default function FormCompleted() {
  return (
  <PageContainer>
    <FormValueDisplay />
  </PageContainer>
  );
}